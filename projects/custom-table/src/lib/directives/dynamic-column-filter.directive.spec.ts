import { DynamicColumnFilterDirective } from './dynamic-column-filter.directive';

describe('DynamicColumnFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicColumnFilterDirective();
    expect(directive).toBeTruthy();
  });
});
