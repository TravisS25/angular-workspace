import { ColumnCheckboxDirective } from './column-checkbox.directive';

describe('ColumnCheckboxDirective', () => {
  it('should create an instance', () => {
    const directive = new ColumnCheckboxDirective();
    expect(directive).toBeTruthy();
  });
});
