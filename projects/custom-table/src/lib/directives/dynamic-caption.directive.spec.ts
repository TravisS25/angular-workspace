import { DynamicCaptionDirective } from './dynamic-caption.directive';

describe('DynamicCaptionDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicCaptionDirective();
    expect(directive).toBeTruthy();
  });
});
