import { DynamicBodyCellDirective } from './dynamic-body-cell.directive';

describe('DynamicBodyCellDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicBodyCellDirective();
    expect(directive).toBeTruthy();
  });
});
