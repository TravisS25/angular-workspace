import { DynamicTabPanelDirective } from './dynamic-tab-panel.directive';

describe('DynamicTabPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicTabPanelDirective();
    expect(directive).toBeTruthy();
  });
});
