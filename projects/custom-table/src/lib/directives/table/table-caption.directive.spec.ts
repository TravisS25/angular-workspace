import { TableCaptionDirective } from './table-caption.directive';

describe('TableCaptionDirective', () => {
  it('should create an instance', () => {
    const directive = new TableCaptionDirective();
    expect(directive).toBeTruthy();
  });
});
