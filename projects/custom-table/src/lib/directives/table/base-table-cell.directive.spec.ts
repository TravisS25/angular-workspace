import { BaseTableCellDirective } from './base-table-cell.directive';

describe('BaseTableCellDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseTableCellDirective();
    expect(directive).toBeTruthy();
  });
});
