import { TableOutputTemplateDirective } from './table-output-template.directive';

describe('TableOutputTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new TableOutputTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
