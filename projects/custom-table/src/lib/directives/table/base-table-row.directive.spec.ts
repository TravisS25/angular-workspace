import { BaseTableRowDirective } from './base-table-row.directive';

describe('BaseTableRowDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseTableRowDirective();
    expect(directive).toBeTruthy();
  });
});
