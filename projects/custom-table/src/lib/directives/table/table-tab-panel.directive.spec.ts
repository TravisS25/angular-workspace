import { TableTabPanelDirective } from './table-tab-panel.directive';

describe('TableTabPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new TableTabPanelDirective();
    expect(directive).toBeTruthy();
  });
});
