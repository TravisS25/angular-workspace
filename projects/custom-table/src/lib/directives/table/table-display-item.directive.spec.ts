import { TableDisplayItemDirective } from './table-display-item.directive';

describe('TableDisplayItemDirective', () => {
  it('should create an instance', () => {
    const directive = new TableDisplayItemDirective();
    expect(directive).toBeTruthy();
  });
});
