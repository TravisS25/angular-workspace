import { DisplayItemDirective } from './display-item.directive';

describe('DisplayItemDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayItemDirective();
    expect(directive).toBeTruthy();
  });
});
