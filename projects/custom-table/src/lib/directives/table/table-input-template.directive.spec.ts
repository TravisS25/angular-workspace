import { TableInputTemplateDirective } from './table-input-template.directive';

describe('TableInputTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new TableInputTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
