import { MobileTablePanelTitleDirective } from './mobile-table-panel-title.directive';

describe('MobileTablePanelTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileTablePanelTitleDirective();
    expect(directive).toBeTruthy();
  });
});
