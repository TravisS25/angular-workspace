import { MobileTablePanelDescriptionDirective } from './mobile-table-panel-description.directive';

describe('MobileTablePanelDescriptionDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileTablePanelDescriptionDirective();
    expect(directive).toBeTruthy();
  });
});
