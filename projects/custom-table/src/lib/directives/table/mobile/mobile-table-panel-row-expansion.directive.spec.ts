import { MobileTablePanelRowExpansionDirective } from './mobile-table-panel-row-expansion.directive';

describe('MobileTablePanelRowExpansionDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileTablePanelRowExpansionDirective();
    expect(directive).toBeTruthy();
  });
});
