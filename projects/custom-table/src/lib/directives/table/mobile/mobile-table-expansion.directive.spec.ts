import { MobileTableExpansionDirective } from './mobile-table-expansion.directive';

describe('MobileTableExpansionDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileTableExpansionDirective();
    expect(directive).toBeTruthy();
  });
});
