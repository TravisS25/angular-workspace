import { MobileTableCaptionDirective } from './mobile-table-caption.directive';

describe('MobileTableCaptionDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileTableCaptionDirective();
    expect(directive).toBeTruthy();
  });
});
