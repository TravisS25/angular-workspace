import { BaseMobileTableDirective } from './base-mobile-table.directive';

describe('BaseMobileTableDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseMobileTableDirective();
    expect(directive).toBeTruthy();
  });
});
