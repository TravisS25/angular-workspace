import { MobileTableExpansionPanelDirective } from './mobile-table-expansion-panel.directive';

describe('MobileTableExpansionPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileTableExpansionPanelDirective();
    expect(directive).toBeTruthy();
  });
});
