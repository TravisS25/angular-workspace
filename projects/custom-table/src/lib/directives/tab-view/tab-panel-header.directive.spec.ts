import { TabPanelHeaderDirective } from './tab-panel-header.directive';

describe('TabPanelHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new TabPanelHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
