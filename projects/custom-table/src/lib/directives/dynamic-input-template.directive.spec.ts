import { DynamicInputTemplateDirective } from './dynamic-input-template.directive';

describe('DynamicInputTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicInputTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
